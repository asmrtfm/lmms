TASK: Fix ZynAddSubFX no-sound and phantom automation track
STATUS: Fixes implemented, awaiting user testing

== COMPLETED THIS SESSION ==

1. ZynAddSubFX element ordering fix (ROOT CAUSE: JSON key alphabetical sorting)
   - Problem: QJsonObject iterates keys alphabetically, destroying XML element order
   - ZynAddSubFX's XML parser is order-sensitive; reordered elements produce no sound
   - Fix in XmlToSqlite.cpp: elemToJson() now stores _order array recording every child
     element tag in sequence (including repeats for interleaved elements)
   - Fix in SqliteToXml.cpp: jsonToXml() uses _order to emit elements in original order,
     consuming array elements one at a time via sequential index tracking
   - Refactored jsonToXml into two functions: jsonKeyToXml (single key) + jsonToXml (full obj)
   - Verified: 29/29 ZynAddSubFX instances have PERFECT element ordering match

2. Phantom automation track fix (ROOT CAUSE: type 6 exported as type 5, wrong container)
   - Problem: Song-level HiddenAutomation track (type 6) was stored without container info
   - On export, placed into trackcontainer as type 5, making it visible in song editor
   - Fix: Added container_type (TEXT, 'song_tc'|'song') and track_type (INTEGER) columns
     to automation_track table
   - XmlToSqlite: Stores container_type and track_type from original XML
   - SqliteToXml: Exports automation tracks to correct location with correct type
   - Backwards-compatible: Old DBs without new columns still work (fallback logic)
   - Verified: 0 automation tracks in trackcontainer, 1 at song level with type=6

3. Test improvements
   - SqliteRoundTripTest now supports LMMS_ROUNDTRIP_MMP env var for custom .mmp path
   - SouthendRevivalAgain.mmp: 0 tag diffs, 1,108,398 matching attributes
   - All 7 tests pass

Files changed:
- src/core/XmlToSqlite.cpp (elemToJson _order, container_type, track_type)
- src/core/SqliteToXml.cpp (jsonToXml ordering, automation track export)
- tools/lmms_schema.sql (automation_track schema)
- tests/src/SqliteRoundTripTest.cpp (env var support)

== STILL TODO ==
- User must test ZynAddSubFX sound with fresh .lmms-db from .mmp
- Duplicate instrument track name handling (not yet implemented)
- Mixer channel import remapping (export side done, import side TODO)
- Song-level controller export ordering (controllers now after UI elements)
